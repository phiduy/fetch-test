import styled from "styled-components"

export const TextDisabled = styled.p`
  color: var(--disabled);
  font-size: 14px;
  margin-bottom: 8px;
`

export const Link = styled.a`
  display: flex;
  align-items: center;
  padding: 8px 0;
  color: var(--blue);
`
